This folder keeps the pre-React prototype files for reference only.

Files here are not the current app entry point.
The active app now lives in the Vite + React project at the repository root (`src/`, `index.html`, `package.json`).

Moved legacy files:
- `Planner.html`
- `otherViews.jsx`
- `store.jsx`
- `yearView.jsx`
- `DESIGN-apple.md`
